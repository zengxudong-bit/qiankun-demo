#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../.store/cross-env@7.0.3/node_modules/cross-env/src/bin/cross-env-shell.js" "$@"
  ret=$?
else 
  node  "$basedir/../.store/cross-env@7.0.3/node_modules/cross-env/src/bin/cross-env-shell.js" "$@"
  ret=$?
fi
exit $ret
