#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../../../resolve@1.22.1/node_modules/resolve/bin/resolve" "$@"
  ret=$?
else 
  node  "$basedir/../../../../../../resolve@1.22.1/node_modules/resolve/bin/resolve" "$@"
  ret=$?
fi
exit $ret
